# django_core/requirements.txt
# 버전 고정 — 배포 환경에서 로컬과 동일한 동작 보장

Django==5.2.15
psycopg2-binary==2.9.12
djangorestframework==3.17.1
django-cors-headers==4.9.0
django-filter==25.2
django-import-export==4.4.1
python-dotenv==1.2.2
redis==8.0.1
openai==2.44.0

# ==================== Phase 2: 인증 ====================
django-allauth[socialaccount]==65.18.0   # Google OAuth2 (의존성 포함)
python3-openid==3.2                      # allauth Steam(OpenID) provider 의존성
djangorestframework-simplejwt==5.5.1     # JWT 토큰
requests==2.34.2                         # allauth Google provider
cryptography==49.0.0                     # allauth JWT 처리
pyjwt[crypto]==2.13.0                    # JWT 암호화

# ==================== 배포 (운영 서버) ====================
gunicorn==23.0.0        # WSGI 서버 (runserver는 개발 전용)
whitenoise==6.9.0       # 정적 파일 서빙 (admin CSS/JS)